pipeline {
    agent any
    tools { 
        maven 'Maven 3.6.1' 
        jdk 'jdk8' 
    }
    stage ('Build') {
        steps {
            echo 'This is a minimal pipeline.'
            
        }
    }
}